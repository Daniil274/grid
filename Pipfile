[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
openai-agents = ">=0.2.5"
httpx = ">=0.25.0"
openai = ">=1.0.0"
pydantic = ">=2.0.0"
pyyaml = ">=6.0"
python-dotenv = ">=1.0.0"
pathlib = ">=1.0.0"
pytest = ">=7.0.0"
pytest-asyncio = ">=0.21.0"
black = ">=23.0.0"
flake8 = ">=6.0.0"
structlog = ">=23.0.0"
click = ">=8.0.0"
rich = ">=13.0.0"

[dev-packages]

[requires]
python_version = "3.12"
