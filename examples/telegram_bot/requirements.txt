# Unified Agent Bot - Telegram Server Requirements
# Установка: pip install -r requirements-telegram.txt

# Telegram Bot API
python-telegram-bot>=21.0

# OpenAI и Agents SDK (если еще не установлен)
openai>=1.0.0

# YAML конфигурация
pyyaml>=6.0

# Системные метрики для health monitoring
psutil>=5.9.0

# Опционально: для улучшенного логирования
# loguru>=0.7.0

# Голосовые сообщения (STT + TTS)
torch>=2.0.0            # Silero TTS (на RPi/ARM: pip install torch после основного окружения)
faster-whisper>=1.0.0   # Whisper STT (CUDA или CPU)
pydub>=0.25.0           # WAV→OGG (нужен ffmpeg в PATH: apt install ffmpeg)
scipy>=1.10.0           # wavfile для Silero TTS
soundfile>=0.12.0       # аудио I/O
transliterate>=1.10.0   # латиница → кириллица для Silero
# Модель Silero: speech-text/model.pt (скачать отдельно, см. README/docs)
