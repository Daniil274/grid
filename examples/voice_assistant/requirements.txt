# Голосовой ассистент — дополнительные зависимости
# Основные (faster-whisper, pydub, scipy, torch) — в requirements-telegram.txt

sounddevice>=0.4.0   # Захват микрофона + воспроизведение через PortAudio
soundfile>=0.12.0    # Чтение WAV/OGG аудиофайлов для воспроизведения
pyautogui>=0.9.0     # Управление мышью и клавиатурой

# Системные требования:
# - ffmpeg в PATH (для pydub WAV→OGG и как fallback плеер)
# - PortAudio (для sounddevice):
#   Windows: устанавливается автоматически с sounddevice
#   Linux/RPi: sudo apt install libportaudio2
