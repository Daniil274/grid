# GRID Agent System API Requirements
agents
# Core FastAPI dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic[email]==2.5.0

# Authentication and security
python-jose[cryptography]==3.3.0
PyJWT==2.8.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# HTTP client
httpx==0.25.2
aiohttp==3.9.0

# System monitoring
psutil==5.9.6

# JSON handling
orjson==3.9.10


# WebSocket and real-time support
websockets==12.0
python-socketio==5.10.0

# CORS and middleware
# CORS is supported via starlette, which is a dependency of fastapi

# Development dependencies
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-mock==3.12.0
httpx==0.25.2  # For testing

# Optional dependencies for production
redis==5.0.1
uvloop==0.19.0  # For better async performance on Linux
gunicorn==21.2.0  # For production deployment

# Core AI framework (required for AgentsSDK)
agents

# Exclude TensorFlow dependencies
# tensorflow  # Not needed - using API-based models only