# GRID Agent System API Requirements (cleaned)

# Core FastAPI stack (modern, Py3.13-compatible)
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
pydantic>=2.10.0,<3.0.0
email-validator>=2.1.0

# Auth and security
PyJWT>=2.9.0,<3.0.0
passlib[bcrypt]>=1.7.4,<2.0.0
python-multipart>=0.0.9,<0.1.0

# HTTP and async
psutil>=5.9.6,<6.0.0

# Optional (install in production only)
# redis>=5.0.1
# uvloop>=0.19.0  # Linux only
# gunicorn>=21.2.0  # Linux only