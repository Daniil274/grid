Create a new openclaude_dev_system for programming based on claude-tools. The system should consist of the following agents: orchestrator (entry point, receives tasks and coordinates), reviewer (checks code and artifacts), tester (runs tests and returns a report), context_collector (gathers context from files and memory). Describe prompts, agents, tools and providers in bundle artifacts at path workspace/openclaude/. Create local_tools.py with the openclaude_dev_system.validate_plan tool, which verifies that the plan contains review and test steps. After creation, register the system, promote to stable, and run a test call system_invoke_system with the task "write a sorting function in Python".